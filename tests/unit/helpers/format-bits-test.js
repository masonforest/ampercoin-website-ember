import {
  formatBits
} from 'ampercoin-website-ember/helpers/format-bits';

module('FormatBitsHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatBits(42);
  ok(result);
});
