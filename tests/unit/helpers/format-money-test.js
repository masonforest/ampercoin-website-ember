import {
  formatMoney
} from 'ampercoin-website-ember/helpers/format-money';

module('FormatMoneyHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatMoney(42);
  ok(result);
});
