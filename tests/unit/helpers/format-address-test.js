import {
  formatAddress
} from 'ampercoin-website-ember/helpers/format-address';

module('FormatAddressHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatAddress(42);
  ok(result);
});
