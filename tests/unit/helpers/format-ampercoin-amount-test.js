import {
  formatAmpercoinAmount
} from 'ampercoin-website-ember/helpers/format-ampercoin-amount';

module('FormatAmpercoinAmountHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatAmpercoinAmount(42);
  ok(result);
});
