import {
  bitsToUsd
} from 'ampercoin-website-ember/helpers/bits-to-usd';

module('BitsToUsdHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = bitsToUsd(42);
  ok(result);
});
