import {
  formatMbtc
} from 'ampercoin-website-ember/helpers/format-mbtc';

module('FormatMbtcHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatMbtc(42);
  ok(result);
});
